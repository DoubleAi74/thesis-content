\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\newcommand{\figureflag}[3]{\par\medskip\noindent
\fcolorbox{orange!70!black}{orange!6}{\parbox{0.94\linewidth}{%
\textbf{\textcolor{orange!50!black}{FIGURE FLAG #1 --- #2}}\\[2pt]#3}}%
\par\medskip}
\begin{document}
\section{Over-long flag test}
\figureflag{F4a.7}{Chained transfer}{\textit{Purpose.} Long specification. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. Every curve, formula and parameter value is stated here in full so a later figure agent needs no other context. }
\end{document}
