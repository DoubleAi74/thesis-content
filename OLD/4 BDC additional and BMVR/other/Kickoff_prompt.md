# Kickoff — Three-Chapter BDC Build

You are the execution agent for a long, autonomous build. Working directory:
`/Users/adamaldridge/Desktop/Thesis content 🎓 /4 BDC additional and BMVR/`

**Mission.** Build three polished, standalone, final-draft-quality thesis
chapters — `BDC_core/` (Chapter 3), `BDC_extra/` (Chapter 4a), `BDC_odes/`
(Chapter 4b) — from the existing sources in this directory, exactly as
specified by the handoff package.

**Start here, in this order:**

1. Read `Startup.md` — overview, current state, guardrails, definition of done.
2. Read `Project_specification.md` — the binding specification: unified
   notation, per-chapter construction specs, the figure-flag system, the
   verified formula reference, the prose standard.
3. Read `Progress.md` — the phased task breakdown. This is your live tracking
   and recovery document; keep it updated continuously.

Then read the sources those documents direct you to — including
`Grant_paper.pdf` in full (first-class source and register model) — and
execute the phases in order.

**Execution mode: fully autonomous, end to end.** The user is unavailable
overnight and will not reply until the work is done. Therefore:

- Continue until the definition of done (`Startup.md` §7) is fully met and
  every checkbox in `Progress.md` is checked. Do not stop early, and do not
  stop for the day.
- **Never ask questions and never wait for approval** — no pilots, no
  stylistic gates, no interim reviews. The plan has already been audited;
  every decision is either settled in the documents or trusted to your
  judgment.
- When you meet an ambiguity, resolve it in light of the settled decisions
  and the specification, and record your choice in `Progress.md` Log A.
- When you meet a genuine blocker, work around it where possible, record it
  in `Progress.md`, and continue with whatever is unblocked. Never idle.
- Save your work at every logical milestone and update `Progress.md`
  continuously (completed work, current work, next actions, decisions,
  formula changes, figure/build status, verification results) — another
  agent must be able to resume from it at any moment.
- Obey the guardrails (`Startup.md` §5): write only inside the three build
  folders and the handoff documents; never modify the source folders, the
  notes, or the verification suites; re-run the suites after any
  formula-level change and record the results.

**Quality bar.** The finished chapters must read as final-draft chapters of a
rigorous, high-quality interdisciplinary applied-mathematics PhD thesis —
prose per `/aa-flow-lucid` with `Grant_paper.pdf` as the register model
(thesis prose is naturally more expansive than the paper, never looser),
the unified notation applied everywhere, figure flags with full generation
specs in place of new figures (you generate no figures yourself).

Begin.
