# F4a.6 remaining steps

Human scientific resolution is required before production:

1. Decide whether the intended claim is restricted to early infection ages,
   concerns cumulative release/yield rather than instantaneous flux, or uses
   a different conditioning/normalisation of `g_k`.
2. Update the authoritative scientific text and brief consistently if the
   intended claim differs from the current immutable statement.
3. Re-run `generate.py` after replacing the final pointwise-order assertion
   with the approved, scientifically precise assertion.
4. Generate `figures/F4a_6_moi.pdf`, inspect the preview, and integrate it
   beside the still-visible F4a.6 flag.
