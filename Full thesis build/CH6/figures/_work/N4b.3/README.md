# N4b.3 build record

- Medium: standalone TikZ; all production content remains vector.
- Scientific ordering: qualitative concentration of release across cell age,
  without asserting a false ordering of mean yield.
- HIV is outside the BDC strip and explicitly uses its own release kernel.
- Numerical asserts: not applicable (schematic brief).
